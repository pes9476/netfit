# 박준범의 NetFit 프로젝트 종합 개발 회고록

---

## 📋 프로젝트 기본 정보
- **팀명**: NetFit (KSPO 국민체육진흥공단 공공데이터 경진대회)
- **담당자**: 박준범
- **완성일(제출일)**: 2026-09-25
- **GitHub 저장소**: [https://github.com/pes9476/netfit](https://github.com/pes9476/netfit)
- **기술 스택**: Python 3.13, Django 5.x, PostgreSQL (Supabase / Railway), HTML5/CSS3/Vanilla JS, Railway Cloud Platform

---

# 📅 날짜별 상세 개발 회고록

---

## 2026-09-14 (월) · 프로젝트 착수 및 카카오 소셜 인증 체계 구축

### 1. 당시 상황 & 배경
- **무엇을 하려고 했는가?**: Django 기반의 웹 애플리케이션 초기 골격을 구성하고, 카카오 소셜 로그인을 통해 사용자가 별도의 복잡한 회원가입 절차 없이 1초 만에 서비스에 진입할 수 있도록 인증 체계를 구축하고자 했다.
- **왜 필요했는가?**: KSPO 경진대회 서비스 특성상 사용자의 진입 장벽(회원가입 번거로움)을 최소화해야 했으며, 모바일과 웹 어디서나 간편하게 접근 가능한 카카오 OAuth 인증 기반이 필수적이었다.

### 2. 실제 구현 과정
1. Django 프로젝트 및 `fitness` 메인 코어 애플리케이션 생성.
2. Kakao Developers REST API 키 발급 및 인가 코드 콜백 엔드포인트(`/login/kakao/callback/`) 구현.
3. Django 기본 `User` 및 1:1 관계의 `Profile` 모델 구축.
4. 비밀번호 기반의 레거시 회원가입 폼을 숨기고 카카오 로그인 전용 단일 진입 경로로 라우팅 정리.
5. `test_kakao.py`, `test_web_flow.py` 단위 테스트를 작성하여 인증 흐름 및 비로그인 접근 차단 검증.

### 3. 발생한 문제
- **증상/에러**: 카카오로 처음 가입한 사용자의 경우 화면 상단과 친구 목록에 난수 형태의 임시 ID(`kakao_3847291...`)나 '운동친구'라는 어색한 기본값이 노출됨.
- **원인 분석**: 카카오 OAuth에서 비즈앱 권한이 없는 경우 사용자의 실명이나 카카오 계정 닉네임 조회가 제한되어 Django `User.username`에 고유 ID를 임시로 저장했기 때문.

### 4. 해결 방법
- **구체적 해결책**: `Profile.display_name` 프로퍼티에서 임시 난수 ID 패턴을 필터링하여 사용자 맞춤 닉네임이 없을 경우 빈 문자열을 반환하도록 하고, 상단 네비게이션에 `[닉네임 설정]` 안내 배지를 띄워 프로필 페이지에서 원하는 별명을 직접 등록하도록 유도함.
- **선택 이유**: 카카오 API의 개인정보 권한 문제를 우회하면서도 사용자에게 친근한 커스텀 닉네임을 유도할 수 있는 가장 직관적인 UX 방식임.

### 5. 작동 원리 & 기술 설명
- **인증 워크플로우**:
  1. 사용자가 `/login/`에서 [카카오 로그인] 클릭.
  2. 서버(`kakao.py`)에서 CSRF 방지용 `state` 토큰을 세션에 저장하고 카카오 인가 서버로 리다이렉트.
  3. 인가 코드 수신 후 카카오 토큰 발급 API 및 사용자 정보 API 호출.
  4. 고유 ID(`kakao_id`)를 기반으로 기존 회원 조회 또는 신규 회원 생성 후 Django 표준 세션(`auth_login`) 생성.
- **연결 구조**: 카카오 OAuth API ↔ `kakao.py` ↔ `views.py(kakao_callback)` ↔ `models.py(Profile)`

### 6. 코드/파일
- `fitness/kakao.py`: 카카오 OAuth 토큰 요청 및 회원 정보 수신 유틸
- `fitness/views.py`: `kakao_login`, `kakao_callback` 뷰
- `fitness/models.py`: `Profile`, `KakaoAccount` 모델
- `fitness/templates/fitness/login.html`: 소셜 로그인 진입 화면

### 7. 완료 상태
- **완료**

---

## 2026-09-15 (화) · NetFit 네온 테마, 마스코트 4종 구축 및 튼튼머니 온보딩 연동

### 1. 당시 상황 & 배경
- **무엇을 하려고 했는가?**: NetFit만의 독창적인 브랜드 아이덴티티를 확립하기 위해 4종 운동 마스코트(백호, 포동, 토리, 아콩)를 제작 및 배치하고, 국민체육진흥공단(KSPO)의 대표 복지 정책인 **스포츠인센티브(튼튼머니)** 안내 팝업 및 개인 맞춤 온보딩 설문을 구현하고자 했다.
- **왜 필요했는가?**: 단순한 운동 일지 앱을 넘어 게이미피케이션(캐릭터 성장)과 국가 공공 지원금(튼튼머니 최대 5만 포인트) 혜택을 연계해 지속적인 운동 참여 동기를 부여해야 했다.

### 2. 실제 구현 과정
1. 생성형 AI 도구를 활용해 4종 캐릭터의 배경을 완전 투명화한 2048×2048 고해상도 CSS 스프라이트 아틀라스(`netfit-mascots.png`) 제작.
2. 다크 네이비(`#0b1329`) 배경에 민트 포인트 컬러를 적용한 `netfit-theme.css` 및 마스코트 애니메이션 전용 `netfit-mascots.css` 구축.
3. 서비스 첫 진입 시 스포츠인센티브(튼튼머니) 5만원 환급 안내 팝업(`teunteun_popup.html`) 구현.
4. 온보딩 6단계(서비스 소개 → 솔로/파티 모드 선택 → 운동 코스 추천 → 초기 미션 설정 → 신체 스펙 등록) 흐름 개발.

### 3. 발생한 문제
- **증상/에러**: 초기 기획에서 사용자의 인바디(BMI) 측정값에 따라 마스코트가 강제로 변경(예: 비만형은 특정 캐릭터)되어 사용자가 심리적 거부감을 느낌. 또한 4개 캐릭터 이미지를 개별 로딩할 때 화면 깜빡임 발생.
- **원인 분석**: 체형에 따른 자동 배정 로직이 사용자 자율성을 침해했고, 다수의 PNG 이미지 요청으로 인한 네트워크 오버헤드가 발생함.

### 4. 해결 방법
- **구체적 해결책**: 캐릭터는 프로필에서 사용자가 자유롭게 고를 수 있도록 수동 선택 방식으로 변경하고, 4종 캐릭터를 1장의 2×2 스프라이트 시트로 통합한 후 CSS `background-position`으로 렌더링.
- **선택 이유**: 사용자 만족도를 극대화하고 단 한 번의 이미지 요청으로 모든 캐릭터를 즉시 캐싱하여 렌더링 성능을 획기적으로 개선함.

### 5. 작동 원리 & 기술 설명
- **스프라이트 아틀라스 원리**: 2048px 시트를 4분할(1024×1024)하여 좌상단(백호 `0 0`), 우상단(포동 `-100% 0`), 좌하단(토리 `0 -100%`), 우하단(아콩 `-100% -100%`) 좌표를 클래스별로 매핑.
- **온보딩 워크플로우**: `onboarding_intro` → `onboarding_mode` → `onboarding_solo/group` → `onboarding_profile` 순으로 세션 데이터를 유지하며 마지막에 `User.profile.onboarding_completed=True`로 영구 저장.

### 6. 코드/파일
- `fitness/static/fitness/css/netfit-theme.css`: 네온 다크 테마 시스템
- `fitness/static/fitness/css/netfit-mascots.css`: 캐릭터 스프라이트 및 모션 스타일
- `fitness/templates/fitness/teunteun_popup.html`: 튼튼머니 지원금 홍보 모달
- `fitness/templates/fitness/onboarding_*.html`: 온보딩 단계별 템플릿

### 7. 완료 상태
- **완료**

---

## 2026-09-16 (수) · 운동 배지 시스템, 아웃핏 상점 및 공공체육시설 데이터 파이프라인 구축

### 1. 당시 상황 & 배경
- **무엇을 하려고 했는가?**: 운동 수행 시간에 따른 3단계 배지(브론즈/실버/골드) 보상 체계를 구축하고, 획득한 포인트로 마스코트를 꾸밀 수 있는 아웃핏 상점을 구현함과 동시에 KSPO 전국 공공체육시설 데이터(`facilities.csv`)를 DB에 적재하여 주변 시설 추천 기능을 개발하고자 했다.
- **왜 필요했는가?**: 운동을 할수록 캐릭터가 시각적으로 화려해지는 보상 피드백을 제공하고, 공공데이터를 직접 활용해 내 주변의 무료/저렴한 공공 운동 시설을 찾아갈 수 있도록 돕기 위함.

### 2. 실제 구현 과정
1. `BadgeAward` 모델 설계: 30분 운동 시 브론즈(30pt), 60분 은(60pt), 90분 금(100pt) 자동 산정.
2. `OutfitPurchase` 모델 및 상점 페이지(`outfit_shop.html`) 구현: 모자(CAP), 왕관(CROWN), 스포츠웨어(SPORT) 구매 및 장착 기능.
3. Django Management Command `import_facilities.py` 작성: 7만여 건의 공공체육시설 CSV 데이터를 읽어 위도/경도/시설유형을 `Facility` 테이블로 일괄 인덱싱.
4. 사용자의 현재 위치(HTML5 Geolocation) 또는 선택 지역 기반으로 인근 시설을 거리순 정렬하는 알고리즘 구현.

### 3. 발생한 문제
- **증상/에러**: 하루에 동일한 미션을 여러 번 클릭했을 때 배지와 포인트가 무한 지급되는 버그 발생, 대용량 CSV 임포트 시 메모리 부족 및 타임아웃 발생.
- **원인 분석**: 중복 완료 방지 유니크 검증 로직 누락, CSV 파싱 시 한 줄씩 `save()`를 호출하여 DB I/O 병목 발생.

### 4. 해결 방법
- **구체적 해결책**:
  - `BadgeAward` 생성 전 `personal_quest` 및 `awarded_at` 날짜 기준 중복 체크 로직 삽입.
  - CSV 임포터에 `bulk_create(batch_size=1000)` 방식을 적용하여 적재 시간을 10분에서 15초로 단축.
- **선택 이유**: 게이미피케이션 재화의 인플레이션을 차단하고, 배포 환경에서 안정적인 데이터 시딩(Seeding)을 보장하기 위함.

### 5. 작동 원리 & 기술 설명
- **게이미피케이션 사이클**: 운동 기록 제출(`record_workout`) → 시간 검증 → `BadgeAward` 발행 → `Profile.badge_points` 증가 → 아웃핏 상점에서 결제 → `Profile.equipped_outfit` 변경 → 대시보드 마스코트 위에 악세서리 레이어 오버레이.

### 6. 코드/파일
- `fitness/models.py`: `BadgeAward`, `OutfitPurchase`, `Facility` 모델
- `fitness/management/commands/import_facilities.py`: 공공데이터 대량 적재 커맨드
- `fitness/templates/fitness/outfit_shop.html`: 캐릭터 커스터마이징 상점
- `fitness/templates/fitness/facilities.html`: 주변 공공체육시설 탐색 뷰

### 7. 완료 상태
- **완료**

---

## 2026-09-17 (목) · 스포츠 뉴스 롤링 배너, 안전한 로그아웃 모달 및 용어 전면 표준화

### 1. 당시 상황 & 배경
- **무엇을 하려고 했는가?**: 대시보드 상단에 최신 스포츠 소식을 실시간으로 보여주는 동적 롤링 배너를 추가하고, 사용자의 실수로 인한 로그아웃을 방지하는 확인 모달을 제작하며, 서비스 전반의 '퀘스트(Quest)' 용어를 사용자 친화적인 '미션(Mission)'으로 일괄 통일하고자 했다.
- **왜 필요했는가?**: 메인 대시보드의 시각적 정보성을 높이고, 모바일 터치 미스로 세션이 끊기는 불편을 없애며, 게임 용어에 익숙하지 않은 중장년층 사용자까지 아우르기 위함.

### 2. 실제 구현 과정
1. CSS Keyframes 기반 세로 방향 텍스트 롤링 배너 컴포넌트 제작(마우스 오버 시 일시 정지 기능 포함).
2. Django 5.x의 보안 권장 사항에 맞추어 GET 방식의 단순 링크 로그아웃을 폐기하고, Bootstrap 모달(`logout_modal.html`) 내 POST + CSRF 토큰 폼으로 개편.
3. 템플릿과 뷰 전반에 걸쳐 '일일 퀘스트', '주간 퀘스트' 표기를 '일일 미션', '주간 미션'으로 일괄 리팩토링.

### 3. 발생한 문제
- **증상/에러**: 텍스트 롤링 시 글자가 아래에서 위로 올라오다가 마지막 줄에서 뚝 끊기며 첫 줄로 튀는 시각적 결함 발생.
- **원인 분석**: CSS 무한 롤링을 구현할 때 첫 번째 아이템의 복제본을 리스트 끝에 배치하지 않아 keyframe 100% 도달 시 리셋 지연이 눈에 보임.

### 4. 해결 방법
- **구체적 해결책**: 리스트의 첫 번째 뉴스 아이템을 맨 마지막에 복제(Clone) 요소로 배치하고, `animation-timing-function: cubic-bezier`를 적용해 자연스러운 루프를 완성함.

### 5. 작동 원리 & 기술 설명
- **안전 로그아웃 메커니즘**:
  - `base.html`의 로그아웃 버튼 클릭 → JS가 `#logoutModal` 오픈.
  - 모달의 [로그아웃] 최종 확인 클릭 시 `method="post" action="{% url 'logout' %}"` 전송.
  - Django의 `LogoutView`가 CSRF 토큰을 검증하고 세션을 안전하게 무효화한 뒤 홈 화면으로 리다이렉트.

### 6. 코드/파일
- `fitness/templates/fitness/logout_modal.html`: 로그아웃 확인 팝업
- `fitness/templates/fitness/dashboard.html`: 스포츠 뉴스 롤링 배너 및 미션 섹션
- `fitness/templates/fitness/base.html`: 전역 네비게이션 및 모달 인클루드

### 7. 완료 상태
- **완료**

---

## 2026-09-18 (금) · 대시보드 미션 허브 개편, 운동 자동 타이머 연동 및 시연 준비

### 1. 당시 상황 & 배경
- **무엇을 하려고 했는가?**: 대시보드의 세로 스크롤 압박을 줄이기 위해 '컴팩트 미션 허브(탭 전환형 솔로/파티 뷰)'를 신설하고, 미션 카드에서 [시작] 버튼을 누르면 해당 운동 종목과 목표 시간이 즉시 세팅되는 인터랙티브 타이머 모달을 연동하고자 했다.
- **왜 필요했는가?**: "미션을 보다가 운동 기록 페이지로 따로 넘어가서 다시 시간을 입력해야 하는 번거로움"을 해소하여, 한 화면에서 원클릭으로 운동을 측정하고 인증할 수 있는 직관적인 UX를 제공하기 위함.

### 2. 실제 구현 과정
1. 대시보드 상단 미션 섹션을 `솔로 미션` / `파티 내기 미션` 탭 구조로 재설계.
2. 각 미션 카드 내에 [미션 시작] 버튼 추가 및 `data-workout-type`, `data-target-minutes` 데이터 속성 바인딩.
3. 바닐라 JS 스톱워치/타이머 모듈 작성: 목표 시간 달성 시 사운드 효과 및 자동 운동 기록 폼 전송 모달 오픈.
4. 일일/주간 미션의 목표 달성률을 한눈에 볼 수 있는 인터랙티브 프로그레스 바 구현.
5. 중간 발표 및 심사를 위한 PPT 이미지와 README 마크다운 문서 최신화.

### 3. 발생한 문제
- **증상/에러**: 모바일 뷰에서 탭을 전환할 때 카드가 화면 밖으로 삐져나오거나, 타이머 실행 도중 화면이 꺼지면 `setInterval`이 느려져 실제 운동 시간과 불일치하는 문제.
- **원인 분석**: 절대 좌표와 고정 너비(px) 사용으로 인한 모바일 뷰포트 오버플로우, 브라우저 백그라운드 스로틀링(Throttling)으로 인한 타이머 틱 오차.

### 4. 해결 방법
- **구체적 해결책**:
  - Flexbox 기반 `flex-wrap` 및 CSS `clamp()` 단위로 모바일 반응형 완벽 대응.
  - 단순 `setInterval` 카운팅 방식 대신 `Date.now()` 타임스탬프 기반 델타(경과 시간) 계산 방식으로 변경하여 백그라운드에서도 오차 없는 정확한 시간 측정 구현.

### 5. 작동 원리 & 기술 설명
- **타이머 연동 워크플로우**:
  1. 미션 카드 [시작] 클릭 → `startMissionTimer(type, minutes)` 호출.
  2. 타이머 모달 오픈 및 밀리초 단위 정밀 타이머 가동.
  3. 일시정지, 재개, 완료 처리 지원.
  4. [운동 완료 및 인증] 클릭 시 운동 기록 엔드포인트(`record_workout`)로 시간과 종목이 자동 전달되어 원스톱 기록 완료.

### 6. 코드/파일
- `fitness/templates/fitness/dashboard.html`: 컴팩트 미션 허브 및 탭 뷰
- `fitness/templates/fitness/party_challenge_modals.html`: 미션 타이머 및 인증 모달
- `README.md`: 프로젝트 기능 소개 문서

### 7. 완료 상태
- **완료**

---

## 2026-09-21 (월) · Railway 운영 배포, 파티 내기 역전 가이드/폭죽, 브랜치 인수인계, 카드배틀 제거 & 실시간 스마트 폴링 알림 완성

### 1. 당시 상황 & 배경
- **무엇을 하려고 했는가?**:
  1. Railway 클라우드 플랫폼에 PostgreSQL(Supabase) 및 Gunicorn 기반으로 실제 운영 환경 배포를 구축.
  2. 친구들과 함께하는 파티 내기의 박진감을 높이기 위해 **D-Day 임박 알림, 1위 맹추격 경고, 추격자 역전 가이드, 1위 우승 시 폭죽(Confetti) 연출** 구현.
  3. C1/C2 개발 PC 간 작업 충돌을 막기 위해 **개발 브랜치(`testsv`)와 운영 배포 브랜치(`runsv`)를 명확히 분리**하고 지침 문서(`GEMINI.md`) 수립.
  4. 이전 기획에서 폐기되었으나 친구 목록에 남아 혼란을 주던 **카드 배틀 잔재 코드 완전 제거**.
  5. 친구 추가, 파티 초대, 콕 찌르기를 보냈을 때 **상대방이 페이지를 새로고침하지 않아도 화면에 즉시 알림이 뜨는 실시간 시스템 구축**.

### 2. 실제 구현 과정
1. **클라우드 배포 파이프라인**:
   - `railway.json`, `Procfile` 구성 및 `psycopg3` 드라이버 적용, Supabase Session Pooler 연동.
2. **파티 내기 역전 가이드 & 1등 폭죽**:
   - 파티 내기 참여자들의 누적 점수를 실시간 집계하여 1위에게는 "2위 OO님이 단 50점 차로 맹추격 중! 🔥", 2위에게는 "50점만 더 따면 1위 역전 가능! ⚡" 가이드를 동적 산출.
   - 챌린지 종료 시 1위 달성 사용자 화면에 Canvas Confetti 폭죽 효과 자동 발사.
3. **브랜치 인수인계 체계 확립**:
   - `testsv` 브랜치를 기본 작업 및 upstream으로 지정, `runsv`는 배포 승인 시에만 fast-forward 병합하여 운영 안정성 확보.
4. **카드 배틀 잔재 제거**:
   - `friends.html`에서 카드 배틀 시작 버튼 및 관련 모달 마크업 삭제.
5. **실시간 스마트 폴링 알림 시스템 개발**:
   - **백엔드**: `/api/notifications/` 신설 (미확인 콕 찌르기, 파티 초대/수락, 친구 요청/수락 5개 항목 집계).
   - **AJAX 비동기 처리**: 알림 수락/거절/확인 엔드포인트에 `X-Requested-With: XMLHttpRequest` 감지 시 JSON 응답 지원.
   - **프론트엔드**: `base.html`에 10초 간격 스마트 폴링, Page Visibility API(화면 나갈 때 절전), 우측 상단 플로팅 토스트 팝업(`showToast`), 대시보드 동적 카드 렌더링 및 페이드아웃 애니메이션 구현.
6. **검증 및 동기화**:
   - `test_notifications_api_polling_and_ajax_dismiss` 단위 테스트 작성 (전체 49개 테스트 100% 통과).
   - 미러 저장소 동기화 및 `testsv` → `runsv` 운영 서버 최종 배포 완료.

### 3. 발생한 문제
- **이슈 1 (배포 에러)**: Railway 배포 중 `psycopg2-binary`가 Python 3.13 환경에서 C-extension 컴파일 에러를 일으키며 빌드 실패.
- **이슈 2 (인프라 한계)**: 실시간 알림을 위해 Django Channels(WebSockets) 도입을 검토했으나, Redis 브로커가 필요하고 Railway 무료 티어의 Gunicorn 워커가 영구 연결로 인해 고갈될 위험 발생.
- **이슈 3 (UX 결함)**: 알림을 수락하거나 닫을 때마다 페이지 전체가 새로고침되어 화면이 하얗게 깜빡거림.
- **이슈 4 (배포 혼선)**: VS Code에서 `git push`를 했으나 실제 웹사이트에 수정 사항이 반영되지 않음 (개발 브랜치 `testsv`로 푸시되었고, 배포 브랜치는 `runsv`였던 원인).

### 4. 해결 방법
- **해결 1**: 최신 공식 PostgreSQL 어댑터인 `psycopg[binary] >= 3.1` (psycopg3)로 교체하여 컴파일 의존성 없이 즉시 빌드 성공.
- **해결 2**: 추가 서버 비용과 워커 고갈 위험이 없는 **'Page Visibility 연동 경량 JSON 스마트 폴링(10s)'** 구조 채택. 탭 비활성화 시 폴링을 멈추고 탭 복귀 시 즉시 1회 체크하여 트래픽 최소화.
- **해결 3**: 뷰 함수들이 AJAX 요청을 감지하면 `JsonResponse({"success": True})`를 반환하도록 개선하고, 프론트엔드에서 카드를 부드럽게 페이드아웃(`opacity: 0`) 시킨 후 DOM에서 삭제.
- **해결 4**: `testsv`의 커밋을 `runsv`로 fast-forward push하여 배포 트리거를 정상 가동시키고 개발 브랜치 분리 원리를 사용자에게 명확히 안내.

### 5. 작동 원리 & 기술 설명
- **실시간 알림 아키텍처**:
  ```
  [클라이언트 브라우저 (모든 페이지)]
     │ (10초 주기 백그라운드 fetch / 탭 활성화 시 즉시 실행)
     ▼
  [GET /api/notifications/]
     │ (Django ORM 최적화 쿼리)
     ▼
  { total_count, unread_pokes, accepted_party_invitations, ... } JSON 응답
     │
     ├─▶ 새 알림 감지 시: 우측 상단 플로팅 토스트(Toast) 팝업 노출
     └─▶ 대시보드 화면인 경우: #notificationAlertsContainer에 알림 카드 동적 삽입
  ```
- **비동기 액션 워크플로우**:
  - 사용자가 [수락/거절/확인] 클릭 → `handleNotificationAction()` 인터셉트 → `fetch(action, {headers: {'X-Requested-With': 'XMLHttpRequest'}})` → DB 업데이트 → 해당 카드 0.3초 트랜지션 페이드아웃 제거.

### 6. 코드/파일
- `fitness/views.py`: `notifications_api` 신설 및 기존 알림 처리 뷰 AJAX 응답 확장
- `fitness/urls.py`: `path("api/notifications/", notifications_api)` 등록
- `fitness/templates/fitness/base.html`: 전역 스마트 폴링, 토스트 컨테이너, 비동기 핸들러
- `fitness/templates/fitness/dashboard.html`: 알림 컨테이너 ID 및 동적 카드 템플릿
- `fitness/templates/fitness/friends.html`: 카드 배틀 버튼 제거
- `fitness/test_web_flow.py`: 실시간 폴링 및 비동기 처리 테스트 케이스
- `GEMINI.md`: Git 브랜치 운용 지침서

### 7. 완료 상태
- **완료**

---

# 🏆 마일스톤과 최종 정리

## 1. 프로젝트 전체를 통해 구현한 주요 기능들
1. **간편 소셜 온보딩 & 사용자 맞춤화**:
   - 카카오 1초 로그인, 사용자 자율 선택형 4종 마스코트, 신체 스펙 기반 온보딩 설문.
2. **KSPO 공공데이터 연계 서비스**:
   - 전국 7만여 공공체육시설 위치 기반 검색, 거리순 정렬 및 날씨 맞춤형 운동 코스 추천.
   - 국민체육진흥공단 스포츠인센티브(튼튼머니) 5만원 환급 정책 연계 안내.
3. **게이미피케이션 운동 인증 & 성장 시스템**:
   - 30분/60분/90분 운동에 따른 브론즈/실버/골드 배지 및 포인트 적립.
   - 적립된 포인트로 나만의 마스코트를 꾸밀 수 있는 아웃핏(모자, 왕관, 스포츠웨어) 상점.
4. **소셜 인터랙션 & 파티 내기 챌린지**:
   - 친구 추가, 상호 콕 찌르기(응원하기).
   - 파티 내기 생성, 1위 맹추격 알림, 2위 역전 가이드, D-Day 임박 카운트다운, 우승자 폭죽 애니메이션.
5. **실시간 스마트 알림 & 컴팩트 UX**:
   - 새로고침이 필요 없는 10초 스마트 폴링 및 플로팅 토스트 팝업.
   - 깜빡임 없는 비동기(AJAX) 알림 처리.
   - 운동 자동 타이머와 일체화된 컴팩트 대시보드 미션 허브.

---

## 2. 가장 어려웠던 부분과 해결 과정

### ① 실시간 양방향 알림 구현과 서버 자원 최적화의 딜레마
- **도전 과제**: 새로고침 없이 친구 요청이나 콕 찌르기를 즉시 알려주어야 했으나, 클라우드(Railway)의 제한된 환경에서 WebSocket(Django Channels + Redis)을 구동하기에는 연결 유지 비용과 프로세스 과부하 위험이 컸음.
- **돌파구**: 브라우저의 **Page Visibility API**와 결합한 **경량 스마트 폴링(Smart Polling)** 기법을 고안함. 사용자가 다른 탭을 볼 때는 통신을 완전히 중단하여 서버 부하를 0으로 만들고, 탭으로 돌아오는 순간 즉시 데이터를 갱신함으로써 인프라 비용 없이 실시간 웹소켓과 동일한 사용자 경험을 제공함.

### ② 여러 개발 환경(C1, C2)과 클라우드 배포 파이프라인의 Git 브랜치 관리
- **도전 과제**: 다수의 작업자가 서로 다른 PC에서 개발을 진행하고, Railway 자동 배포 브랜치(`runsv`)와 로컬 개발 브랜치가 얽히면서 최신 코드가 누락되거나 배포가 즉시 반영되지 않는 혼선이 발생함.
- **돌파구**: `GEMINI.md` 프로젝트 표준 지침서를 수립하여 기본 개발 및 upstream은 `testsv`로 단일화하고, 검증이 끝난 안정 버전만 `runsv`로 fast-forward 병합하여 푸시하는 체계적인 배포 규칙을 확립함.

---

## 3. 배운 기술과 개념
- **Full-Stack Django & RESTful AJAX 패턴**: 템플릿 렌더링 방식의 Django에서 `JsonResponse`와 바닐라 자바스크립트의 `fetch` API를 조합하여 화면 깜빡임 없는 모던 SPA(Single Page Application) 수준의 UX를 구축하는 방법을 체득함.
- **클라우드 PaaS 배포 및 Python 3.13 최적화**: Railway, Supabase PostgreSQL, Gunicorn, WhiteNoise 정적 파일 서빙 파이프라인을 구축하고, 최신 Python 3.13 환경에서 `psycopg3`를 활용한 데이터베이스 연결 풀링 최적화 기법을 학습함.
- **CSS 스프라이트 및 프론트엔드 렌더링 최적화**: 다수의 이미지를 단일 아틀라스 시트로 결합하여 HTTP 요청 횟수를 75% 절감하고, Canvas API 및 CSS 트랜지션을 활용한 동적 애니메이션 제어 능력을 배양함.
- **공공데이터 가공 및 대용량 배치 처리**: KSPO 공공체육시설 CSV 원시 데이터를 정제하고 `bulk_create`를 통해 수만 건의 레코드를 수초 내에 DB로 인덱싱하는 데이터 엔지니어링 기초를 다짐.

---

## 4. 다음에 개선할 점 (Future Roadmap)
1. **GPS 기반 실시간 야외 운동 트래킹**:
   - 현재 수동 입력 및 타이머 측정 중심에서, 스마트폰 GPS 경로를 지도(카카오맵 API) 위에 실시간으로 시각화하는 기능 추가.
2. **AI 비전 기반 운동 인증 사진 자동 검증**:
   - 사용자가 업로드한 헬스장/러닝 인증 사진을 Google Gemini Vision API를 통해 자동 판독하여 허위 인증을 원천 차단하고 보상을 자동 지급하는 파이프라인 고도화.
3. **카카오톡 알림톡(메시지 API) 연동**:
   - 웹 브라우저를 닫고 있을 때도 친구의 내기 초대나 콕 찌르기가 카카오톡 메시지로 직접 도착할 수 있도록 카카오 비즈니스 메시징 API 확장.
